// Tied to GitHub Pages project-page hosting (served under /maison-fleurie/).
// Remove this prefix if the site later moves to a custom domain or root-path host.
export const BASE_PATH = "/maison-fleurie";
